\begin{table}[htbp]
\centering
\begin{tabular}{llllll}
\hline
check_id & description & status & risk_level & evidence & explanation \\
\hline
L1 & All features at time t use only information available at or before t & PASS & none & src/scie_revision/common.py::chronological_train_val_test_split & Chronological split and lagged features only; no future lookahead. \\
L2 & The 3-day-ahead target is never used in feature scaling & PASS & none & src/prism/data/preprocess.py::preprocess_dataset & Scaling is fit on the training subset only and excludes the target column. \\
L3 & Scaling parameters are fitted on training data only, then applied to val/test & PASS & none & src/prism/data/preprocess.py::preprocess_dataset & Train-only means and standard deviations; no data leakage from future. \\
L4 & Rolling statistics (volatility, sentiment) use only past/current observations & PASS & none & src/prism/data/preprocess.py::preprocess_dataset & Rolling features are built with lag/shift operations ensuring causality. \\
L5 & Graph construction uses training-period data only for each split & PASS & none & src/graph/risk_aware_graph.py::build_risk_aware_graph & Graph helper consumes the training panel only; test data never enter edge score computation. \\
L6 & MIS selection is computed using training-period graph only & PASS & none & src/graph/risk_aware_graph.py::greedy_maximal_independent_set & Selected on the graph returned from training-period data only. \\
L7 & MAML support/query splits are chronological and non-overlapping; test set is fully held out & PASS & none & src/prism/models/prism_variants.py::_train_maml & Support set from early training period, query set from later training period. Test set never touched. \\
L8 & Validation data are not used in meta-training updates except for early stopping & PASS & none & src/prism/models/prism_variants.py::_train_maml & Validation used only for model selection (best checkpoint) and early stopping of meta-learning. \\
L9 & Test data are never used for feature selection, token selection, graph construction, scaling, tuning, early stopping, or meta-training & PASS & none & src/scie_revision/common.py + src/prism/data/preprocess.py & Test rows are held out before any tuning, scaling, or meta-training occurs. \\
L10 & All ablation variants and all baselines use the same token subset, same chronological split, same forecast horizon, and same target transformation & PASS & none & src/prism/models/prism_variants.py + src/models/baselines/ & Ablation variants are derived from the same processed panel with identical splits. All baseline runners receive identical train/val/test frames. \\
\hline
\end{tabular}
\caption{Leakage and chronology validation for the revision pipeline (10-check V6 format).}
\label{leakage_audit_report}
\end{table}